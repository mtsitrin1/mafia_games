# mafia_games

Home for mafia-style social-deduction party games. First title: the
Chameleon game (single-link, single-device web app — see
`docs/ideas/chameleon-party.md` for the finalized PRD/design doc).

This repo has no GitHub remote push yet — it exists only as a local git
repo in the ideation environment, pending push credentials.
